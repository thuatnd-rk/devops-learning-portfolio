# projects/ai-agent-eks/argocd/applications/customer-a/backend-app-production.yaml
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: customer-a-backend-production
  namespace: argocd
  labels:
    customer: customer-a
    service: backend
    environment: production
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: ai-agent
  source:
    repoURL: https://github.com/rookie-ai-agent/backend
    targetRevision: customer-a
    path: kubernetes/overlays/production  # ← Sửa path
  destination:
    server: https://kubernetes.default.svc
    namespace: customer-a-production  # ← Sửa namespace
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true